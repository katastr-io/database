databaseChangeLog:
- changeSet:
    author: Michal Zimmermann <zimmicz@gmail.com>
    id: alter-table-${schema.name}.region_statistics
    dbms: postgresql
    changes:
    - sql:
        sql: |
            ALTER TABLE ${schema.name}.region_statistics DROP COLUMN alike;
            ALTER TABLE ${schema.name}.region_statistics ADD COLUMN alike json;
    - rollback:
        ALTER TABLE ${schema.name}.region_statistics DROP COLUMN alike;
        ALTER TABLE ${schema.name}.region_statistics ADD COLUMN alike integer[];